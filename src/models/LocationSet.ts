export class LocationSet {
    id: string
    name: string
    group: string
    info: string

    constructor(id: string, name: string, group: string, info: string) {
        this.id = id
        this.name = name
        this.group = group
        this.info = info
    }
}